# AI 回复语言设置
# 设置为 'zh' 使用中文回复，设置为 'en' 使用英文回复
VITE_AI_LANGUAGE=zh

# Google Gemini API 密钥（示例占位符，勿填真实值到示例文件）
VITE_GOOGLE_GEMINI_API_KEY=REPLACE_ME

# Auth 模式：mock 表示前端本地模拟登录；生产/后端联调请改为 server
VITE_AUTH_MODE=server

# 开发服务器跨域来源
DEV_ORIGIN=http://localhost:5173

# 阿里云 OSS 配置（填入后启用服务端上传）
# ALIYUN_ACCESS_KEY_ID=REPLACE_ME
# ALIYUN_ACCESS_KEY_SECRET=REPLACE_ME
# ALIYUN_OSS_REGION=oss-cn-hangzhou
# ALIYUN_OSS_BUCKET=your-bucket
# ALIYUN_OSS_PREFIX=uploads/
